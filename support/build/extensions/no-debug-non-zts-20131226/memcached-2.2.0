#!/usr/bin/env bash
# Build Path: /app/.heroku/php/
# Build Deps: extensions/no-debug-non-zts-20131226/memcached-bare-2.2.0, libraries/libmemcached

# That's it - we're combining the dep build and the extension build here

source $(dirname $0)/../no-debug-non-zts-20121212/memcached
