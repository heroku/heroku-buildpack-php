#!/usr/bin/env bash
# Build Path: /app/.heroku/php/
# Build Deps: extensions/no-debug-non-zts-20121212/ssh2-bare, libraries/libssh2

# That's it - we're combining the dep build and the extension build here

echo "-----> Merging dependencies..."
echo "-----> Done."